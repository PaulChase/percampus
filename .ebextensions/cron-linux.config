files:
  "/etc/cron.d/cron_percampus":
    mode: "000644"
    owner: root
    group: root
    content: |
      * * * * * root cd /var/www/html/ && php schedule:run 1>> /var/www/html/cron.log 2>&1

commands:
  rm_old_cron:
    command: "rm -fr /etc/cron.d/cron_example.bak"
    ignoreErrors: true